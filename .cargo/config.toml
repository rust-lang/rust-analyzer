[alias]
xtask = "run --package xtask --bin xtask --"
tq = "test -- -q"
qt = "tq"
lint = "clippy --all-targets -- --cap-lints warn"
codegen = "run --package xtask --bin xtask -- codegen"
dist = "run --package xtask --bin xtask -- dist"

[target.x86_64-pc-windows-msvc]
linker = "rust-lld"

[target.arm-unknown-linux-gnueabihf]
linker = "arm-linux-gnueabihf-gcc-8"

[env]
CARGO_WORKSPACE_DIR = { value = "", relative = true }
# Match the stack size used by rust-analyzer's worker threads.
# Deep trait solving in debug builds can exceed libtest's default stack.
RUST_MIN_STACK = "16777216"
